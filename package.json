{
    "name": "super-agent-monitor",
    "version": "1.0.0",
    "description": "Super Agent Monitor - The Ultimate Claude Code Companion (Mac Native)",
    "scripts": {
        "dev": "./dev.sh",
        "dev:no-open": "./dev.sh --no-open",
        "test": "cd backend && bun test",
        "logs": "tail -f .backend.log .frontend.log",
        "clean": "rm -f .dev-status.json .backend.log .frontend.log"
    }
}