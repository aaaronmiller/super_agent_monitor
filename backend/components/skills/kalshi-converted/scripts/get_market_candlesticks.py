#!/usr/bin/env python3
# /// script
# dependencies = [
#     "click",
#     "mcp",
# ]
# ///

"""
Adapter script for MCP Tool: get_market_candlesticks
Generated by Super Agent Monitor
"""

import sys
import json
import click
from pathlib import Path

# Add original MCP server directory to path
sys.path.append(str(Path("/Users/macuser/git/0MY_PROJECTS/super_agent_monitor/beyond-mcp/apps/1_mcp_server")))

# Import the original module
try:
    import server as original_module
except ImportError as e:
    print(f"Error importing original module: {e}", file=sys.stderr)
    sys.exit(1)

@click.command()
@click.option('--series-ticker', help='series_ticker', type=click.STRING)
@click.option('--market-ticker', help='market_ticker', type=click.STRING)
@click.option('--start-ts', help='start_ts', type=click.INT)
@click.option('--end-ts', help='end_ts', type=click.INT)
@click.option('--interval', help='interval', type=click.INT)
@click.option('--json', 'output_json', is_flag=True, help='Output as JSON')
def main(series_ticker, market_ticker, start_ts, end_ts, interval, output_json):
    """
    Get candlestick data for a specific market.

Args:
    series_ticker: Series ticker containing the market
    market_ticker: Market ticker
    start_ts: Start timestamp (Unix timestamp)
    end_ts: End timestamp (Unix timestamp)
    interval: Period interval in minutes - 1 (1min), 60 (1hr), 1440 (1day)

Returns:
    Dict with 'ticker' and 'candlesticks' array
    """
    try:
        # Call the original function
        # Note: We access the function directly from the module
        # If the function is decorated, we might need to access the underlying function
        # But usually calling the decorated function works if it returns the value
        
        if hasattr(original_module, 'get_market_candlesticks'):
            func = getattr(original_module, 'get_market_candlesticks')
            result = func(series_ticker=series_ticker, market_ticker=market_ticker, start_ts=start_ts, end_ts=end_ts, interval=interval)
            
            if output_json:
                print(json.dumps(result, indent=2))
            else:
                # Simple formatted output
                print(f"Result for get_market_candlesticks:")
                print(json.dumps(result, indent=2))
        else:
            print(f"Function get_market_candlesticks not found in module", file=sys.stderr)
            sys.exit(1)
            
    except Exception as e:
        if output_json:
            print(json.dumps({"error": str(e)}))
        else:
            print(f"Error: {e}", file=sys.stderr)
        sys.exit(1)

if __name__ == "__main__":
    main()
